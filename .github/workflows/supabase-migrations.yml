name: Supabase Migrations

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
  push:
    branches:
      - main

jobs:
  validate-migrations:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      OPENAI_API_KEY: test-openai-key
      SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN: test-twilio-token
      GOOGLE_CLIENT_ID: test-google-client-id
      GOOGLE_CLIENT_SECRET: test-google-client-secret
      SUPABASE_AUTH_EXTERNAL_APPLE_SECRET: test-apple-secret
      S3_HOST: test-s3-host
      S3_REGION: test-s3-region
      S3_ACCESS_KEY: test-s3-access-key
      S3_SECRET_KEY: test-s3-secret-key

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 2.92.1

      - name: Start local Supabase stack
        run: supabase start

      - name: Validate migrations on a fresh local database
        run: supabase db reset

  push-production-migrations:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    concurrency:
      group: supabase-production-migrations
      cancel-in-progress: false
    environment: Action Production

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 2.92.1

      - name: Push migrations to the linked production project
        env:
          DOTENV_PRIVATE_KEY_PRODUCTION: ${{ secrets.DOTENV_PRIVATE_KEY_PRODUCTION }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: bun run supabase:db:push:production
